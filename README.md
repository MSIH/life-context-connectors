# life-context-connectors — folded into life-context

**This repo has been folded into [`MSIH/life-context`](https://github.com/MSIH/life-context) (full history preserved via `git subtree`) and is archived.**

- Connector code now lives at [`life-context/connectors/`](https://github.com/MSIH/life-context/tree/2.0/connectors) — one self-contained folder per connector (`devsession-claude`, `imessage`, `photo-exif`).
- Open issues were transferred to the [core tracker](https://github.com/MSIH/life-context/issues) (old issue URLs redirect).
- Rationale + the still-available re-split path (`git subtree split`): [doc 04 §10](https://github.com/MSIH/life-context/blob/2.0/docs/04-connector-contract.md#10-distribution) and [issue #49](https://github.com/MSIH/life-context/issues/49).

This archive intentionally keeps its native commit history for per-file archaeology (pre-fold paths).
